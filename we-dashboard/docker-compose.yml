services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: we-dashboard
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/data
